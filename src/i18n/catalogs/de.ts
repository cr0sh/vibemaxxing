import type { PartialCatalog } from '../catalog'

/**
 * German catalog contract. Replace this partial export with a complete
 * catalog before enabling German in the registry. Preserve all placeholders.
 */
export const germanCatalog: PartialCatalog = {}
